const { resolve } = require('path');

module.exports = {
  config: resolve('src', 'config', 'context.js'),
  'models-path': resolve('src', 'app', 'models'),
  'seeders-path': resolve('src', 'database', 'seeders'),
  'migrations-path': resolve('src', 'database', 'migrations'),
}